<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MMORPG Blog post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">

    <style>
        /* Add CSS styles for better presentation */
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;

        }

        .faq-item {
            margin-bottom: 20px;
        }

        .question {
            font-weight: bold;
        }
    </style>

</head>

<body>
    <%- include("partial/header.ejs") %>

        <div class="container">
            <form action="/create" method="post">
                <div class="row">
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label for="input1" class="form-label">Enter Game Tag <em>(e.g. Lineage 2)</em></label>
                            <input type="text" name="game" class="form-control" id="input1" required
                                placeholder="Enter text...">
                        </div>
                        <div class="mb-3">
                            <label for="input2" class="form-label">Enter Topic</label>
                            <input type="text" name="topic" class="form-control" id="input2" required
                                placeholder="Enter text...">
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="mb-3">
                            <label for="textarea" class="form-label">Enter the post:</label>
                            <textarea class="form-control" name="content" id="textarea" rows="22" required
                                placeholder="Enter text..."></textarea>
                        </div>
                        <input type="hidden" name="uName" value=<%=userData["uName"] %>>
                        <input type="hidden" name="uEmail" value=<%=userData["uEmail"] %>>
                        <input type="hidden" name="date" id="date" value="">
                        <button class="btn btn-success" type="submit">Post</button>
                    </div>
                </div>

            </form>

        </div>




        <%- include("partial/footer.ejs") %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
                crossorigin="anonymous"></script>
                <script>
                    // Define the getCurrentDateTime function
                    function getCurrentDateTime() {
                        const currentDate = new Date();
                        const year = currentDate.getFullYear();
                        const month = String(currentDate.getMonth() + 1).padStart(2, '0');
                        const day = String(currentDate.getDate()).padStart(2, '0');
                        const hours = String(currentDate.getHours()).padStart(2, '0');
                        const minutes = String(currentDate.getMinutes()).padStart(2, '0');
                        return `${day}.${month}.${year} ${hours}:${minutes}`;
                    }
            
                    // Get the input element
                    const dateTimeInput = document.getElementById('date');
                    // Set the value of the hidden input field to the current date and time
                    dateTimeInput.value = getCurrentDateTime();
                </script>
</body>

</html>